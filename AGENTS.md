# Project instructions

- Scope is this repository only; Windows develops and controls, standard GitHub Linux runners acquire and process.
- Use only free PendulumFlow native V3. Never use other evidence projects as authority.
- The catalog continuation additionally permits first-party Polymarket metadata solely for identity, expected membership, interval bounds, token mapping and listing/existence. It does not permit first-party prices, books, winning outcomes or research observations. Catalog network probes run on standard GitHub Linux; project output must project an explicit metadata allowlist and discard all other response fields.
- The earlier Run-1 continuity investigation authorizes bounded transient book/price_change integrity probes on standard GitHub Linux runners only. No source depth may enter canonical research, published rows or an opaque payload field. Its earlier canonical contract and negative continuity evidence remain unchanged.
- The subsequent user policy adds PENDULUMFLOW_V3_OBSERVED, version 1, separately from future OWN_RECORDER_EXACT. It removes continuity/persistence/actual-first-crossing requirements only for the observed profile. Preserve all earlier contracts, canaries and blocker evidence; expected-target discovery, identity, official resolution, integrity and truthful scope remain required. See docs/observed-profile-v1.md. No production promotion while its narrower source gate is blocked.
- The observed profile uses only new_market, best_bid_ask and market_resolved; earlier transient depth diagnostics do not authorize depth in its canonical acquisition or publication.
- The final Run-1 policy scopes PENDULUMFLOW_V3_OBSERVED certification to the complete content-pinned published PendulumFlow V3 inventory generation. It does not claim exhaustive historical Polymarket listings or that inventory absence means never listed. This supersedes the catalog blocker only for this temporary profile; preserve every earlier blocker as historical evidence. See docs/inventory-scope-policy-v1.json.
- The first complete production projection found native best_bid_ask/market_resolved condition IDs with no classifying new_market record anywhere in the pinned generation. This is a current-generation identity-closure blocker for the finite inventory claim, not a continuity or venue-listing-census requirement. Scheduled acquisition remains disabled until content-bound native mapping or supported native exclusion/day-bound evidence makes every relevant reference decidable. See docs/inventory-identity-capability-gap.md.
- No credentials, private material, live recording, sender, factory, or server connections.
- Preserve exact price/time values and witness-local provenance. Archive receipts are not sender HTTP receipts.
- Certification must fail closed on missing, ambiguous, contradictory or unsupported evidence.
- The final bounded-bootstrap policy authorizes `PENDULUMFLOW_V3_OBSERVED_BOUNDED_V1`.
  It admits only positively mapped, individually valid targets; unresolved conditions are
  `UNRESOLVED_NOT_ADMITTED`, never out of scope, and do not block other valid admissions.
  This profile makes no exhaustive population claim. See `docs/bounded-profile-v1.md`.
- Certified authority requires independently verified content-addressed assets and native immutable Releases.
- Use project-local pinned tooling. Run unit tests, format/lint, strict types, workflow checks and diff checks for changes.
